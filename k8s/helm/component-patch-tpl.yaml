apiVersion: v1
values:
  images:
    debugModeOperator: cloudogu/debug-mode-operator:0.3.0
patches:
  values.yaml:
    manager:
      image:
        registry: "{{ registryFrom .images.debugModeOperator }}"
        repository: "{{ repositoryFrom .images.debugModeOperator }}"
        tag: "{{ tagFrom .images.debugModeOperator }}"
